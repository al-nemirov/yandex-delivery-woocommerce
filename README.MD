# Яндекс Доставка для WooCommerce

Плагин интеграции WooCommerce с [Яндекс Доставкой](https://dostavka.yandex.ru/) через официальный B2B Platform API.

## Возможности

### Способы доставки
- **ПВЗ (самовывоз)** — клиент выбирает пункт выдачи на карте Яндекс
- **ПВЗ с наложенным платежом** — оплата при получении в пункте выдачи
- **Курьер до двери** — доставка по адресу клиента
- **Курьер с наложенным платежом** — оплата курьеру при получении

### Расчёт стоимости
- Динамический расчёт через API Яндекс Доставки (`/api/b2b/platform/pricing-calculator`)
- Передача `platform_station_id` для source (склад) и destination (ПВЗ) — корректный расчёт по тарифу договора
- Фиксированная наценка в рублях / процентная наценка поверх цены API
- Фоллбек на фиксированную стоимость при ошибке API
- Отображение срока доставки в днях
- Кэширование результата в рамках PHP-запроса (yd_self + yd_self_after не дублируют API-вызов)

### Вес и габариты
- Автоматический расчёт из свойств товаров WooCommerce
- Конвертация единиц измерения (кг/г, см/м/мм)
- **Укладка стопкой**: `max(длина) × max(ширина) × сумма(высот)` — реалистичный расчёт для книг и коробок
- Корректное округление: `(int) round()` вместо `(int)` усечения (20.7 → 21, а не 20)
- `total_assessed_price` отправляется в копейках (по спецификации API)
- `minWeight` применяется к отправлению, а не к каждому товару отдельно
- Габариты по умолчанию: к каждому товару или ко всему отправлению

### Выбор ПВЗ на чекауте
- Собственный виджет на Яндекс Картах с кластеризацией точек
- Список ПВЗ с адресами и графиком работы
- Поиск по адресу внутри модалки
- **Цена не показывается пока ПВЗ не выбран** — заглушка «выберите пункт выдачи»
- **Сброс ПВЗ при смене города** — cookies и UI автоматически очищаются
- Валидация: обязательный выбор ПВЗ для самовывоза
- Автозаполнение billing-полей для ПВЗ (адрес, индекс)
- Сохранение выбора в cookies для устойчивости при обновлении чекаута
- Поддержка классического и блочного чекаута WooCommerce

### Подсказки адресов (Dadata)
- Автоподсказки города при вводе
- Автоподсказки адреса для курьерской доставки
- Настройка API-ключа Dadata в отдельном меню

### Выгрузка заказов
- Создание заявки на доставку в Яндекс через API (режим черновика)
- Кнопка «Подтвердить заказ» для ручного подтверждения черновика
- Авто-отправка при переходе в выбранный статус
- Ручная отправка кнопкой из карточки заказа
- Повторная отправка при ошибке (retry)
- Настраиваемый префикс номера заказа
- **`billing_info`**: передача способа оплаты (предоплата / наложенный платёж)
- **`billing_details`**: цены в копейках, НДС для каждого товара
- Сумма наложенного платежа = точная сумма заказа на сайте

### Печать
- Генерация и скачивание ярлыков (PDF)
- Генерация и скачивание актов передачи
- Авто-генерация акта после выгрузки заказа

### Синхронизация статусов и трекинг
- Крон-задача каждые 2 часа — опрос API Яндекс Доставки
- Полная история трекинга сохраняется в мету заказа (без лишних API-запросов)
- Кнопка «Обновить статус» для ручной синхронизации
- Автоматическое обновление статуса WooCommerce-заказа
- Авто-завершение заказа при вручении клиенту
- Время последней синхронизации отображается в мета-боксе

### Интеграция с МойСклад
- Автоматическая выгрузка заказов в МойСклад при смене статуса (как черновик)
- Ручная выгрузка кнопкой из карточки заказа
- Создание контрагентов и заказов покупателей
- Заказы создаются с `applicable: false` — не проводятся автоматически
- Настройка организации и товара по умолчанию

### Администрирование
- Метабокс в карточке заказа: трекинг, способ оплаты, ярлыки, акты, ошибки
- Способ оплаты в мета-боксе: предоплата / наложенный платёж с точной суммой
- Настройка пункта приёма с автокомплитом из API (12000+ точек)
- Проверка валидности API-токена
- **Режим отладки**: чекбокс в настройках метода → логи в `wp-content/yd-debug.log` + debug log в мета-боксе заказа
- Тестовый режим (скрывает методы доставки, не затрагивает боевые заказы)
- Совместимость с HPOS (High-Performance Order Storage, WC 7+)

## Установка

1. Скопируйте папку `yandex-dostavka` в `wp-content/plugins/`
2. Активируйте плагин в админке WordPress
3. Перейдите в **WooCommerce → ЯД — Настройки API** — введите ключ Dadata и Яндекс Карт
4. Перейдите в **WooCommerce → Доставка → Зоны** → добавьте метод «Яндекс Доставка — ПВЗ»
5. Введите OAuth-токен из личного кабинета Яндекс Доставки
6. Выберите пункт приёма заказов (начните вводить город)
7. Настройте наценки и автоматическую отправку

## Требования

- WordPress 5.8+
- WooCommerce 5.0+
- PHP 7.4+
- OAuth-токен Яндекс Доставки (B2B API)

## Сборка (для разработчиков)

Для блочного чекаута WooCommerce:

```bash
cd yandex-dostavka
npm install
npm run build
```

## Архитектура

### Файловая структура

```
yandex-dostavka/
├── yandex-dostavka.php              # Основной файл плагина
│   ├── WC_YD_Parent_Method          # Базовый класс метода доставки
│   ├── WC_YD_Self_Method            # ПВЗ (yd_self)
│   ├── WC_YD_SelfAfter_Method       # ПВЗ + наложенный платёж (yd_self_after)
│   ├── WC_YD_Courier_Method         # Курьер (yd_courier)
│   ├── WC_YD_CourierAfter_Method    # Курьер + наложенный платёж (yd_courier_after)
│   ├── calculate_shipping()         # Расчёт стоимости (вызов API)
│   ├── yd_update_callback()         # AJAX: сохранение выбранного ПВЗ
│   └── action_woocommerce_checkout_update_order_review()  # Очистка кэша
├── includes/
│   └── class-yandex-delivery-api.php  # API-клиент Яндекс Доставки
│       ├── calculate_price()          # POST /api/b2b/platform/pricing-calculator
│       ├── get_pickup_points()        # POST /api/b2b/platform/pickup-points/list
│       ├── create_request()           # POST /api/b2b/platform/request/create
│       ├── confirm_request()          # POST /api/b2b/platform/request/confirm
│       └── generate_labels()          # POST /api/b2b/platform/request/generate-labels
├── js/
│   ├── yandex-dostavka.js           # Логика чекаута: ПВЗ-виджет, подсказки, переключение полей
│   ├── yd-pvz-widget.js             # Виджет выбора ПВЗ на Яндекс Картах
│   └── yandex-dostavka-admin.js     # Админка: выбор пункта приёма
├── src/
│   └── index.js                     # React-компонент для блочного чекаута
├── css/
│   └── yandex-dostavka.css          # Стили кнопки и виджета
└── dist/
    └── bundle.js                    # Сборка React для блочного чекаута
```

### Расчёт стоимости — как работает

> **Покупатель на чекауте → `calculate_shipping()`**

**1. Собираем вес и габариты из корзины**
- Укладка стопкой: `max(L) × max(W) × Σ(H)`
- Округление: `(int)round(value * dimensionCoeff)`

**2. Определяем source (склад)**
- `reception_point` → `getReceptionPointCodeByName()` → `platform_station_id`
- Источник: таблица БД `yd_reception_points`

**3. Определяем destination (ПВЗ)**
- Нет cookie `yd_pvz_code` → заглушка «выберите пункт выдачи», `return`
- Есть cookie → `platform_station_id`

**4. Проверяем `$GLOBALS` кэш** (`md5` параметров)
- Есть → используем кэш (без API-вызова)
- Нет → вызываем API ↓

**5. POST** `/api/b2b/platform/pricing-calculator`

```json
{
  "source": { "platform_station_id": "0199f267..." },
  "destination": { "platform_station_id": "0193c0b8..." },
  "tariff": "self_pickup",
  "total_assessed_price": 244960,
  "total_weight": 1264,
  "places": [{ "physical_dims": { "dx": 21, "dy": 14, "dz": 6, "weight_gross": 1264 } }]
}
```

> `total_assessed_price` — в **копейках**, `total_weight` — в **граммах**

**6. Ответ** → `{"pricing_total": "226.5 RUB", "delivery_days": 3}`
- Финальная цена = `pricing_total` + `addcost` (₽) + `markup_percent` (%) → `add_rate()`

### Кэширование и инвалидация

| Уровень | Механизм | Когда инвалидируется |
|---------|----------|---------------------|
| PHP-запрос | `$GLOBALS['yd_pricing_cache']` | Каждый новый HTTP-запрос |
| WC Session | `shipping_for_package_*` | При каждом `update_order_review` + при смене ПВЗ (`yd_update_callback`) |
| WC Package Hash | `yd_pvz_code` в пакете | Автоматически при смене cookie |
| Браузер JS | Версии скриптов (`?ver=2.50`) | При обновлении плагина |

### Cookies

| Cookie | Устанавливается | Используется | Сбрасывается |
|--------|----------------|-------------|-------------|
| `yd_pvz_code` | JS (виджет) + PHP (`yd_update`) | `calculate_shipping()` → `platform_station_id` | При смене города, при переключении на курьера |
| `yd_pvz_address` | JS (виджет) + PHP (`yd_update`) | `calculate_shipping()` → `destinationAddress` | При смене города, при переключении на курьера |

## Настройки метода доставки

| Параметр | Описание | По умолчанию |
|----------|---------|-------------|
| `key` | OAuth-токен Яндекс Доставки | — |
| `reception_point` | Пункт приёма (склад отправки) | — |
| `addcost` | Фиксированная наценка (₽) | 0 |
| `markup_percent` | Процентная наценка (%) | 0 |
| `fixed_cost` | Фоллбек-цена при ошибке API (₽) | 350 |
| `default_weight` | Вес по умолчанию (г) | 500 |
| `default_height/width/depth` | Габариты по умолчанию (см) | 10 |
| `apply_default_dimensions` | 0=не применять, 1=к товарам без габаритов, 2=ко всему отправлению | 0 |
| `min_weight` / `max_weight` | Фильтр по весу (г) | 0 / 30000 |
| `order_prefix` | Префикс номера заказа в API | — |
| `order_status_send` | Статус для автоотправки | — |

## Debug-логирование

При `WP_DEBUG=true` плагин пишет в `wp-content/debug.log`:

```
[YD] === CALC REQUEST (yd_self) ===
[YD] source=Москва, Судостроительная, 59 (station_id=0199f267...)
[YD] dest=Мытищи, Стрелковая, 4 (station_id=0193c0b8...)
[YD] tariff=self_pickup, weight=1264g
[YD] dimensions: L=21 W=14 H=6 cm
[YD] assessed_price=244960 kopecks (2449.6 RUB)
[YD API] pricing-calculator REQUEST: {"source":{"platform_station_id":"..."}, ...}
[YD API] pricing-calculator RESPONSE: {"pricing_total":"226.5 RUB","delivery_days":3}
[YD] === CALC CACHED (yd_self_after) === reusing result from previous method
```

Для подавления Notice-спама от других плагинов добавьте в `wp-config.php`:
```php
error_reporting( E_ALL & ~E_NOTICE & ~E_DEPRECATED );
```

## Changelog

### v2.11.0

#### Исправлено
- **Обновления из GitHub в админке WordPress**: запрос к API с корректным `User-Agent`; выбор последнего **опубликованного** релиза из списка (учитываются и **pre-release**, не только «последний стабильный» через `releases/latest`, который prerelease игнорирует). Если обновление не показывалось — проверьте на GitHub, что создан именно **Release**, а не только git-тег.

### v2.6.1

#### Добавлено
- **Режим черновиков**: заказы создаются в ЯД без автоподтверждения, кнопка «Подтвердить заказ» для ручного подтверждения
- **`billing_info`** — обязательное поле API ЯД (обновление API 2025+):
  - `payment_method: "already_paid"` — для предоплаченных заказов
  - `payment_method: "card_on_receipt"` — для наложенного платежа
  - `delivery_cost` — стоимость доставки в копейках (точная сумма с сайта)
- **`billing_details`** в каждом товаре: `unit_price`, `assessed_unit_price` (копейки), `nds`
- **Способ оплаты в мета-боксе**: показывается перед созданием заказа и после — предоплата (зелёная галочка) или наложенный платёж (сумма к оплате клиентом = точная сумма заказа на сайте)
- **Трекинг из ЯД**: полная история доставки сохраняется в мету заказа, отображается в мета-боксе без лишних API-запросов
- **Кнопка «Обновить статус»** — ручная синхронизация одного заказа
- **Отдельный лог `wp-content/yd-debug.log`** — как у YooKassa, не смешивается с общим debug.log
- **Чекбокс «Режим отладки»** в настройках метода доставки — логирует все API-запросы/ответы, debug log в мета-боксе заказа (раскрывающийся блок)
- **МойСклад: режим черновиков** — заказы создаются с `applicable: false` (не проводятся автоматически)
- **Поддержка HPOS** (WC 7+) — хуки `woocommerce_update_order` и `save_post_shop_order`, nonce формат `update-order_ID`

#### Исправлено
- Ошибка `billing_info: Field is missing` при создании заказа (обязательное поле API ЯД)
- Кнопки мета-бокса не работали в HPOS-режиме (неправильный nonce и отсутствующий хук)
- Убран невалидный параметр `auto_accept` из запроса create_request
- Автосмена статуса WC и генерация акта перенесены на этап подтверждения (не при создании черновика)

### v2.2.0 — v2.2.3

#### Исправлено
- `total_assessed_price` и `unit_price` в items теперь считаются с НДС (tax inclusive)
- GitHub Auto-Updater: очистка кэша при force-check, TTL 6 часов

### v2.1.0

**Внимание: отработан и протестирован только тариф `self_pickup` (ПВЗ). Курьерская доставка (`time_interval`) не тестировалась в этой версии.**

#### Исправлено
- **Габариты**: заменён кубический корень (`volume^(1/3)`) на укладку стопкой (`max(L) × max(W) × Σ(H)`) — реалистичный расчёт для книг и коробок
- **Округление габаритов**: `(int) round()` вместо `(int)` усечения — 20.7 теперь 21, а не 20
- **`total_assessed_price`**: отправляется в копейках, а не в рублях (по спецификации API)
- **`minWeight`**: применяется к отправлению, а не к каждому товару
- **`platform_station_id`**: передаётся в `pricing-calculator` для source (склад) и destination (ПВЗ), как при создании заказа — без station_id API считал по общему тарифу (~411₽ вместо ~226₽)
- **Дубль API-вызова**: `yd_self` и `yd_self_after` используют `$GLOBALS` кэш в рамках одного PHP-запроса
- **Дубль `update_checkout`**: `yd-pvz-widget.js` не дублирует вызов если `onSelect` callback задан

#### Добавлено
- Цена не показывается пока ПВЗ не выбран (заглушка «выберите пункт выдачи», cost=0)
- Фильтр `woocommerce_cart_shipping_method_full_label` скрывает «0₽/Бесплатно» для методов без выбранного ПВЗ
- Фильтр `woocommerce_cart_shipping_packages` включает `yd_pvz_code` в хэш пакета для инвалидации кэша WC
- Принудительная очистка `shipping_for_package_*` в `yd_update_callback()` при смене ПВЗ
- Сброс ПВЗ при смене города: функция `ndResetPvzSelection()` очищает cookies и UI при blur на `#billing_city`/`#shipping_city` и при выборе из DaData
- Debug-логирование: параметры запроса, station_id, полный ответ API
- Версии JS скриптов обновлены (`2.50`, `2.7.0`) для сброса браузерного кэша

### v2.0.0
- Полный переход на Yandex Delivery B2B API
- Собственный виджет выбора ПВЗ на Яндекс Картах
- Подсказки адресов через Dadata API
- Фиксированная и процентная наценки
- Интеграция с МойСклад
- Поддержка блочного чекаута WooCommerce
- Совместимость с HPOS
- Автоматическая миграция настроек с предыдущих версий

### v1.0
- Первый релиз

## Лицензия

GPLv2 or later
